version: '3.8'

services:
  curator:
    build: .
    container_name: curator
    ports:
      - "8080:8080"
    environment:
      - RUST_LOG=info
      - THREAD_POOL_SIZE=4
    volumes:
      - ./curator.db:/app/curator.db
    restart: unless-stopped
